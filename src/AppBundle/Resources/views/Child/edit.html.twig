{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Child edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        {#<a href="{{ path('child', { 'parentName':app.user.username, 'childName': entity.name }) }}">#}
            {#Back to the list#}
        <a href="{{ path('child_index', { 'parentName':app.user.username }) }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
